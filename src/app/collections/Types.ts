import { CollectionConfig } from "payload/types";
const Types:CollectionConfig={
      slug:"types",
      fields:[
            {
                  name:"eventType",
                  type:"text",
            }
      ]
}
export default Types